// This file has been generated by Tauri Specta. Do not edit this file manually.

import { invoke as __TAURI_INVOKE } from "@tauri-apps/api/core";

/** Commands */
export const commands = {
	bootstrap: () => __TAURI_INVOKE<Bootstrap>("bootstrap"),
	refreshRepositories: (activeRepositoryId: number | null) => __TAURI_INVOKE<RepositorySummary[]>("refresh_repositories", { activeRepositoryId }),
	refreshRepository: (repositoryId: number) => __TAURI_INVOKE<RepositoryRefresh>("refresh_repository", { repositoryId }),
	setGitPath: (path: string | null) => __TAURI_INVOKE<GitInfo>("set_git_path", { path }),
	saveLayout: (leftWidth: number, rightWidth: number, outputHeight: number) => __TAURI_INVOKE<null>("save_layout", { leftWidth, rightWidth, outputHeight }),
	saveLanguage: (language: Language) => __TAURI_INVOKE<null>("save_language", { language }),
	addRepository: (path: string) => __TAURI_INVOKE<RepositorySummary>("add_repository", { path }),
	initializeRepository: (path: string) => __TAURI_INVOKE<RepositorySummary>("initialize_repository", { path }),
	cloneRepository: (url: string, destination: string) => __TAURI_INVOKE<OperationResult>("clone_repository", { url, destination }),
	relocateRepository: (repositoryId: number, path: string) => __TAURI_INVOKE<RepositorySummary>("relocate_repository", { repositoryId, path }),
	updateRepository: (repository: RepositoryRecord) => __TAURI_INVOKE<null>("update_repository", { repository }),
	reorderRepositories: (placements: RepositoryPlacement[]) => __TAURI_INVOKE<null>("reorder_repositories", { placements }),
	saveGroupOrder: (groups: string[]) => __TAURI_INVOKE<null>("save_group_order", { groups }),
	removeRepository: (repositoryId: number) => __TAURI_INVOKE<null>("remove_repository", { repositoryId }),
	watchRepository: (repositoryId: number) => __TAURI_INVOKE<null>("watch_repository", { repositoryId }),
	getStatus: (repositoryId: number, includeIgnored: boolean) => __TAURI_INVOKE<WorkingTreeSnapshot>("get_status", { repositoryId, includeIgnored }),
	getDiff: (repositoryId: number, snapshotId: number, path: string, staged: boolean) => __TAURI_INVOKE<DiffFile>("get_diff", { repositoryId, snapshotId, path, staged }),
	getConflictDocument: (repositoryId: number, snapshotId: number, path: string) => __TAURI_INVOKE<ConflictDocument>("get_conflict_document", { repositoryId, snapshotId, path }),
	getHistory: (repositoryId: number, cursor: {
	offset: number,
	activeLanes: string[],
} | null, limit: number) => __TAURI_INVOKE<CommitPage>("get_history", { repositoryId, cursor, limit }),
	exportSessionLog: (fileName: string, lines: SessionLogLine[]) => __TAURI_INVOKE<boolean>("export_session_log", { fileName, lines }),
	getCommitDetail: (repositoryId: number, oid: string) => __TAURI_INVOKE<CommitDetail>("get_commit_detail", { repositoryId, oid }),
	compareBranches: (repositoryId: number, base: string, head: string) => __TAURI_INVOKE<string>("compare_branches", { repositoryId, base, head }),
	openRepositoryFile: (repositoryId: number, path: string) => __TAURI_INVOKE<null>("open_repository_file", { repositoryId, path }),
	getBranches: (repositoryId: number) => __TAURI_INVOKE<BranchInfo[]>("get_branches", { repositoryId }),
	getTags: (repositoryId: number) => __TAURI_INVOKE<TagInfo[]>("get_tags", { repositoryId }),
	getRemotes: (repositoryId: number) => __TAURI_INVOKE<RemoteInfo[]>("get_remotes", { repositoryId }),
	getStashes: (repositoryId: number) => __TAURI_INVOKE<StashInfo[]>("get_stashes", { repositoryId }),
	getSubmodules: (repositoryId: number) => __TAURI_INVOKE<SubmoduleInfo[]>("get_submodules", { repositoryId }),
	getRebaseCommits: (repositoryId: number, onto: string) => __TAURI_INVOKE<RebaseCommit[]>("get_rebase_commits", { repositoryId, onto }),
	getFileHistory: (repositoryId: number, path: string) => __TAURI_INVOKE<FileHistoryEntry[]>("get_file_history", { repositoryId, path }),
	getCommitFileDiff: (repositoryId: number, oid: string, path: string) => __TAURI_INVOKE<string>("get_commit_file_diff", { repositoryId, oid, path }),
	getBlame: (repositoryId: number, path: string) => __TAURI_INVOKE<BlameFile>("get_blame", { repositoryId, path }),
	previewOperation: (repositoryId: number, request: OperationRequest) => __TAURI_INVOKE<OperationPreview>("preview_operation", { repositoryId, request }),
	startOperation: (repositoryId: number, request: OperationRequest, confirmed: boolean) => __TAURI_INVOKE<OperationResult>("start_operation", { repositoryId, request, confirmed }),
	cancelOperation: (operationId: number) => __TAURI_INVOKE<null>("cancel_operation", { operationId }),
};

/* Types */
export type BlameFile = {
	path: string,
	content: string[],
	hunks: BlameHunk[],
};

export type BlameHunk = {
	oid: string,
	author: string,
	authorTime: number,
	startLine: number,
	lineCount: number,
};

export type Bootstrap = {
	git: GitInfo,
	settings: Settings,
	repositories: RepositorySummary[],
};

export type BranchInfo = {
	name: string,
	oid: string,
	current: boolean,
	remote: boolean,
	upstream: string | null,
};

export type ChangeKind = "added" | "modified" | "deleted" | "renamed" | "copied" | "untracked" | "ignored" | "conflicted" | "typeChanged" | "unknown";

export type CommitDetail = {
	oid: string,
	author: string,
	email: string,
	authoredAt: string,
	message: string,
	files: CommitFileChange[],
};

export type CommitFileChange = {
	path: string,
	originalPath: string | null,
	additions: number | null,
	deletions: number | null,
};

export type CommitInfo = {
	oid: string,
	parents: string[],
	author: string,
	authoredAt: string,
	subject: string,
	refs: string[],
	lane: GraphLane,
};

export type CommitPage = {
	commits: CommitInfo[],
	nextCursor: HistoryCursor | null,
};

export type ConflictChoice = "current" | "incoming" | "both";

export type ConflictDocument = {
	id: string,
	path: string,
	segments: ConflictSegment[],
};

export type ConflictResolution = {
	blockId: string,
	choice: ConflictChoice,
};

export type ConflictSegment = { type: "context"; text: string } | { type: "conflict"; id: string; base: string; current: string; incoming: string };

export type ConflictSide = "ours" | "theirs";

export type DiffFile = {
	path: string,
	staged: boolean,
	binary: boolean,
	tooLarge: boolean,
	patch: string,
	hunks: DiffHunk[],
};

export type DiffHunk = {
	id: string,
	header: string,
	patch: string,
};

export type FileChange = {
	path: string,
	originalPath: string | null,
	kind: ChangeKind,
	indexStatus: string | null,
	worktreeStatus: string | null,
	staged: boolean,
	unstaged: boolean,
	conflict: boolean,
	ignored: boolean,
	additions: number | null,
	deletions: number | null,
};

export type FileHistoryEntry = {
	oid: string,
	author: string,
	authoredAt: string,
	subject: string,
};

export type GitInfo = {
	path: string | null,
	version: string | null,
	supported: boolean,
	error: string | null,
};

export type GraphLane = {
	column: number,
	parentColumns: number[],
};

export type HistoryCursor = {
	offset: number,
	activeLanes: string[],
};

export type Language = "en" | "zh-CN";

export type MergeMode = "fastForward" | "normal" | "squash";

export type OngoingGitState = {
	kind: OngoingKind,
	canContinue: boolean,
	canSkip: boolean,
	canAbort: boolean,
};

export type OngoingKind = "merge" | "rebase" | "cherryPick" | "revert";

export type OperationEvent = {
	operationId: number,
	repositoryId: number | null,
	kind: OperationEventKind,
	message: string,
	exitCode: number | null,
	outcome: OperationOutcome | null,
};

export type OperationEventKind = "started" | "stdout" | "stderr" | "finished";

export type OperationOutcome = "succeeded" | "failed" | "cancelled";

export type OperationPreview = {
	title: string,
	summary: string,
	risk: RiskLevel,
	affectedPaths: string[],
	affectedRefs: string[],
	recoverable: boolean,
	requiresConfirmation: boolean,
};

export type OperationRequest = { type: "stageFiles"; paths: string[] } | { type: "unstageFiles"; paths: string[] } | { type: "stageHunk"; snapshotId: number; hunkId: string } | { type: "unstageHunk"; snapshotId: number; hunkId: string } | { type: "discardTracked"; paths: string[] } | { type: "trashUntracked"; paths: string[] } | { type: "commit"; message: string; amend: boolean; signoff: boolean } | { type: "fetch"; remote: string | null; prune: boolean } | { type: "pull"; strategy: PullStrategy | null } | { type: "push"; remote: string | null; branch: string | null } | { type: "forcePushWithLease"; remote: string; branch: string; expectedOid: string } | { type: "setUpstream"; remote: string; branch: string } | { type: "addRemote"; name: string; url: string } | { type: "setRemoteUrl"; name: string; url: string } | { type: "removeRemote"; name: string } | { type: "deleteRemoteBranch"; remote: string; branch: string } | { type: "createBranch"; name: string; startPoint: string | null; checkout: boolean } | { type: "switchBranch"; name: string } | { type: "renameBranch"; oldName: string; newName: string } | { type: "deleteBranch"; name: string; force: boolean } | { type: "merge"; reference: string; mode: MergeMode } | { type: "rebase"; onto: string } | { type: "interactiveRebase"; onto: string; plan: RebaseStep[] } | { type: "cherryPick"; commits: string[] } | { type: "continue"; kind: OngoingKind } | { type: "skip"; kind: OngoingKind } | { type: "abort"; kind: OngoingKind } | { type: "chooseConflictSide"; path: string; side: ConflictSide } | { type: "markResolved"; paths: string[] } | { type: "resolveConflictBlocks"; snapshotId: number; documentId: string; path: string; choices: ConflictResolution[] } | { type: "stashCreate"; message: string | null; includeUntracked: boolean } | { type: "stashApply"; index: number; pop: boolean } | { type: "stashDrop"; index: number } | { type: "createTag"; name: string; target: string | null; message: string | null } | { type: "deleteLocalTag"; name: string } | { type: "pushTag"; remote: string; name: string } | { type: "submoduleInit"; paths: string[]; recursive: boolean } | { type: "submoduleUpdate"; paths: string[]; recursive: boolean } | { type: "submoduleSync"; paths: string[]; recursive: boolean } | { type: "revert"; oid: string } | { type: "undoLastCommit" } | { type: "runDifftool"; path: string; staged: boolean } | { type: "runMergetool"; path: string | null };

export type OperationResult = {
	operationId: number,
	accepted: boolean,
};

export type PullStrategy = "merge" | "rebase" | "fastForwardOnly";

export type RebaseAction = "pick" | "reword" | "squash" | "fixup" | "drop";

export type RebaseCommit = {
	oid: string,
	subject: string,
	author: string,
};

export type RebaseStep = {
	oid: string,
	action: RebaseAction,
	message: string | null,
};

export type RemoteInfo = {
	name: string,
	fetchUrl: string,
	pushUrl: string,
};

export type RepositoryCapabilities = {
	canRead: boolean,
	canWriteWorkTree: boolean,
	canManageRefs: boolean,
	canManageRemotes: boolean,
};

export type RepositoryKind = "workTree" | "bare" | "missing";

export type RepositoryPlacement = {
	id: number,
	group: string | null,
	favorite: boolean,
	order: number,
};

export type RepositoryRecord = {
	id: number,
	path: string,
	name: string,
	group: string | null,
	favorite: boolean,
	order: number,
};

export type RepositoryRefresh = {
	summary: RepositorySummary,
	snapshot: WorkingTreeSnapshot | null,
};

export type RepositorySummary = {
	id: number,
	path: string,
	name: string,
	group: string | null,
	favorite: boolean,
	order: number,
	kind: RepositoryKind,
	capabilities: RepositoryCapabilities,
	branch: string | null,
	headOid: string | null,
	changedCount: number,
	conflictCount: number,
	ahead: number,
	behind: number,
	lastCommit: string | null,
	ongoing: OngoingGitState | null,
	error: string | null,
};

export type RiskLevel = "normal" | "caution" | "destructive";

export type SessionLogLine = {
	timestamp: string,
	kind: string,
	message: string,
};

export type Settings = {
	gitPath: string | null,
	selectedRepositoryId: number | null,
	leftWidth: number,
	rightWidth: number,
	outputHeight: number,
	language?: Language,
	groupOrder?: string[],
};

export type StashInfo = {
	index: number,
	oid: string,
	subject: string,
};

export type SubmoduleInfo = {
	path: string,
	oid: string,
	initialized: boolean,
	state: string,
};

export type TagInfo = {
	name: string,
	oid: string,
	subject: string,
};

export type WorkingTreeSnapshot = {
	id: number,
	repositoryId: number,
	headOid: string | null,
	files: FileChange[],
};

